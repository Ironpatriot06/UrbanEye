# Example outputs — SYNTHETIC FIXTURES, NOT REAL DATA

Everything here was produced by:

    python tests/make_fixtures.py
    python scripts/run_pipeline.py --fixtures

The inputs were machine-generated by `tests/make_fixtures.py`. **No municipal
portal was contacted.** These files exist so you can see the shape, dtypes and
report format before downloading ~9 GB of real data.

Do not analyse the values. Row counts, distributions and statistics here are
properties of the fixture generator, not of San Francisco, Boston, Chicago or
New York.

Result of that run: 16/16 pipeline steps OK; 11/11 data quality, 11/11
provenance, 11/11 leakage and 15/15 output checks passed.
