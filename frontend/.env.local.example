# Frontend environment
# Copy to .env.local and adjust for your deployment.

# Origin of the UrbanEye+ FastAPI backend.
# Everything — API calls and authenticated image fetches alike — is resolved
# against this value, so there are no hardcoded localhost URLs in the app code.
NEXT_PUBLIC_API_URL=http://localhost:8000
